import FormLogin from "../Fragments/FormLogin";
import Layout from "../Layout/Layout";

const LoginPage = () => {
  return (
    <Layout>
      <FormLogin />
    </Layout>
  );
};

export default LoginPage;
