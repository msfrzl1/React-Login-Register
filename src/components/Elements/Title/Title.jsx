const Title = ({ title }) => {
  return (
    <div className="d-flex justify-content-center py-5 fw-bold">
      <h1 className="fw-bold p-2 rounded">{title}</h1>
    </div>
  );
};

export default Title;
